# OTA Decision Log

> Append at decision points: chose A over B, hit a dead-end, session ending mid-investigation, discovered a non-obvious constraint.
>
> Format: `[OTA-###]` + ISO timestamp + Observation / Thought / Action.
>
> Most recent at bottom. Max 50 entries - drop oldest when exceeded.

---
